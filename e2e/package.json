{
  "name": "cchoice-e2e",
  "private": true,
  "scripts": {
    "prepare:e2e": "bash ../scripts/e2e-seed.sh && bash ../scripts/e2e-build.sh",
    "pretest": "pnpm run prepare:e2e",
    "test": "playwright test",
    "test:ui": "pnpm run prepare:e2e && playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.51.0"
  }
}
