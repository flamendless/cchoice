package components

import (
	"cchoice/cmd/web/components/common"
	"cchoice/internal/constants"
	"cchoice/internal/enums"
	"cchoice/internal/utils"
)

templ ForgotPasswordPage(userType enums.UserType) {
	<!DOCTYPE html>
	<html lang="en" class="overflow-x-hidden">
		<head>
			@common.HeadMeta()
			@common.TabTitle("[FORGOT PASSWORD] C-Choice")
			<style>
				#forgot-btn-content #forgot-spinner { display: none; }
				#forgot-btn-content.htmx-request #forgot-spinner { display: inline-block; }
				#forgot-btn-content.htmx-request .forgot-btn-text { display: none; }
			</style>
		</head>
		<body
			class="bg-cchoicesoft min-h-screen flex items-center justify-center"
		>
			@common.ErrorBanner()
			@common.SuccessBanner()
			<div class="w-full max-w-lg p-8 bg-white rounded-lg shadow-md">
				<a
					href={ utils.URL("/") }
					class="inline-flex items-center gap-1 text-sm text-gray-500 hover:text-gray-700 mb-4"
				>
					<svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
						<path stroke-linecap="round" stroke-linejoin="round" d="M10 19l-7-7m0 0l7-7m-7 7h18"></path>
					</svg>
					Back to Shop
				</a>
				<img
					src={ constants.PathSVGLogoOnly }
					alt="C-Choice Logo"
					class="w-20 h-20 mx-auto mb-4"
				/>
				<h1 class="text-2xl font-bold text-center text-cchoice mb-2">
					Forgot Password
				</h1>
				<p class="text-sm text-gray-600 text-center mb-6">
					Enter your email and we'll send you a link to reset your password
				</p>
				<form
					method="POST"
					action={ utils.URL("/auth/forgot-password") }
					class="space-y-4"
					hx-post={ utils.URL("/auth/forgot-password") }
					hx-trigger="submit"
					hx-indicator="#forgot-btn-content"
				>
					<input type="hidden" name="user_type" value={ userType.String() }/>
					<div>
						<label for="email" class="block text-sm font-medium text-gray-700">
							Email Address
						</label>
						<input
							type="email"
							id="email"
							name="email"
							required
							pattern={ constants.PatternEmail }
							class="mt-1 block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-cchoice focus:border-cchoice"
						/>
					</div>
					<button
						type="submit"
						class="w-full flex justify-center items-center gap-2 py-2 px-4 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-cchoice hover:bg-cchoice_dark focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-cchoice"
					>
						<span id="forgot-btn-content" class="inline-flex items-center gap-2">
							<span id="forgot-spinner" class="w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin shrink-0" aria-hidden="true"></span>
							<span class="forgot-btn-text">Send Reset Link</span>
						</span>
					</button>
				</form>
				<div class="mt-6 text-center text-sm">
					<p class="text-gray-600">
						switch userType {
							case enums.USER_TYPE_STAFF:
								Remember your password?
								<a href={ utils.URL("/admin") } class="text-cchoice hover:text-cchoice_dark font-medium">
									Login here
								</a>
							case enums.USER_TYPE_CUSTOMER:
								Remember your password?
								<a href={ utils.URL("/customer") } class="text-cchoice hover:text-cchoice_dark font-medium">
									Login here
								</a>
						}
					</p>
				</div>
			</div>
		</body>
	</html>
}
