document.addEventListener("DOMContentLoaded", function() {
	const source = document.getElementById("all-category-sections");
	const target = document.getElementById("category-sections");

	// source.addEventListener("scroll", function() {
	// 	target.scrollTop = source.scrollTop;
	// 	console.log("scrolled source");
	// });
	//
	// target.addEventListener("scroll", function() {
	// 	source.scrollTop = target.scrollTop;
	// 	console.log("scrolled target");
	// });

	console.log("scrollbar script loaded");
});
